# Excel Table Design — `Student_Tasks.xlsx`

## 🎯 Purpose
This Excel file acts as the **central database** for all tasks flowing in from Outlook, Calendar, and Canvas.  
All flows (A, B, C, D) read from and write to this table.

The version in this repo is a **template** (`Student_Tasks_Template.xlsx`).  
The *live* version must reside in **OneDrive or SharePoint**.

---

# 📄 Table Name: `Tasks`

The table must be named exactly:


Excel → Table Design → Table Name → `Tasks`

---

# 📊 Column Definitions

Below are the **required columns**, their purpose, and their recommended data types.

| Column | Type | Description |
|--------|------|-------------|
| **TaskId** | Text | Unique identifier generated by Flows (`OUTLOOK-…`, `CAL-…`, `CANVAS-…`) |
| **Title** | Text | Task title from email/Canvas/calendar |
| **Description** | Text | Details, body text, HTML, etc. |
| **Checklist** | Text (multi-line) | Parsed sub-tasks (from Flow A/B/C). Stored as plain text or markdown. |
| **StartDate** | Date/Time | When to start working. Parsed or fallback to `utcNow()`. |
| **DueDate** | Date/Time | Deadline. May be provided or defaulted. |
| **Source** | Text | Outlook / Calendar / Canvas |
| **SourceId** | Text | Raw ID from the source (MessageId, EventId, AssignmentId) |
| **Course** | Text | Course name/code parsed from subject or Canvas |
| **Status** | Text | `New`, `Pushed_to_Planner`, or `Archived` |
| **CreatedOn** | Date/Time | When the task row was created |

---

# 🧠 Data Type Recommendations

### ✔ Date Columns  
Format these as **Date/Time** in Excel:

- `StartDate`
- `DueDate`
- `CreatedOn`

This helps Flow D avoid serial-number date problems.

### ✔ Text Columns  
Use Text type for:

- TaskId  
- Title  
- Description  
- Checklist  
- Source  
- SourceId  
- Course  
- Status  

---

# 🧩 Example Table Layout


---

# 🔧 Additional Notes

### ✔ StartDate Behavior
Flows will fill `StartDate` using:

1. Parsed value (Canvas unlock_at, Calendar event start, etc.)  
2. If missing → fallback:  
   ```text
   utcNow()
